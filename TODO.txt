==URGENT==

==QUICK==
[Both] Allow sending private messages to a single user (ex. !tell <user> <private message>)

==MEDIUM==
[Server] Create new threads for send_to_all (sending would take too long for many concurrent users)

==LONG==
[Both] Set up user authentication
[Both] Instead of console-based, create a GUI
[Both] Encrypt communication for secure chat
